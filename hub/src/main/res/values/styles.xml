<resources>
    <!-- Base application theme. -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!--Toolbar颜色-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--已在hub/res/values/styles.xml中定义actionOverflowMenuStyle-->
        <!--<item name="actionOverflowMenuStyle">@style/MyOverflowMenuStyle</item>-->
    </style>
    <style name="AppTheme" parent="AppBaseTheme">
    </style>

    <!-- Toolbar菜单样式 -->
    <style name="MyToolbarStyle" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--Toolbar背景色，如果定义该属性将一同修改popupTheme的背景色，即使定义了popupTheme的背景色也会无效-->
        <!--<item name="android:background">@color/colorPrimary</item>-->
        <item name="actionOverflowMenuStyle">@style/MyOverflowMenuStyle</item>
        <item name="actionOverflowButtonStyle">@style/MyActionButtonOverflowStyle</item>
    </style>

    <!--ActionBar弹出菜单样式-->
    <style name="MyOverflowMenuStyle" parent="@style/Widget.AppCompat.PopupMenu.Overflow">
        <!-- 是否覆盖锚点，默认为true，即盖住Toolbar -->
        <item name="overlapAnchor">false</item>
        <!-- 弹出层背景颜色 -->
        <item name="android:popupBackground">@color/colorPrimaryDark</item>
        <!-- 弹出层垂直方向上的偏移，即在竖直方向上距离Toolbar的距离，值为负则会盖住Toolbar -->
        <item name="android:dropDownVerticalOffset">5dp</item>
        <!-- 弹出层水平方向上的偏移，即距离屏幕左边的距离，负值会导致右边出现空隙 -->
        <item name="android:dropDownHorizontalOffset">0dp</item>
    </style>

    <!--ActionBar右侧三个点的样式-->
    <style name="MyActionButtonOverflowStyle" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <!--设置三个点的背景色-->
        <item name="android:background">@color/colorAccent</item>
    </style>

    <style name="CollapsingLayoutCollapsedTitle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#ffffff</item>
    </style>
    <style name="CollapsingLayoutExpandedTitle">
        <item name="android:textSize">23sp</item>
        <item name="android:textColor">#ffffff</item>
    </style>
</resources>
